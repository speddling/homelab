apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: tunes
provisioner: rancher.io/local-path
volumeBindingMode: WaitForFirstConsumer
parameters:
  nodePath: /mnt/lab-backups/music-library # Temporary home on the HDD