# Secret for Firecrawl
# This Secret is templated by ansible/roles/firecrawl/tasks/generate-secret.yml
# Values come from ansible/vars/vault.yml
# Do NOT edit manually — regenerate via the deploy-firecrawl.yml workflow
---
apiVersion: v1
kind: Secret
metadata:
  name: firecrawl-secret
  namespace: firecrawl
  labels:
    app: firecrawl
    component: secret
type: Opaque
stringData:
  DATABASE_URL: "postgresql://firecrawl:changeme-postgres@firecrawl-postgres:5432/firecrawl"
  REDIS_PASSWORD: "changeme-redis"
  POSTGRES_PASSWORD: "changeme-postgres"
  BULL_AUTH_KEY: "changeme-api-key"
  SELF_HOSTED_WEBHOOK_HMAC_SECRET: "changeme-webhook"
  TEST_API_KEY: "changeme-test"
  OPENAI_API_KEY: "changeme-openai"
  SLACK_WEBHOOK_URL: ""
  LLAMAPARSE_API_KEY: ""
  SUPABASE_ANON_TOKEN: ""
  SUPABASE_URL: ""
  SUPABASE_SERVICE_TOKEN: ""
  PROXY_PASSWORD: "changeme-proxy"
