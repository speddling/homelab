apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firecrawl-postgres-pvc
  namespace: firecrawl
  labels:
    app: firecrawl
    component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
